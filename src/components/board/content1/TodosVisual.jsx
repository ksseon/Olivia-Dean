import { TodosVisualStyle } from './style';

const TodosVisual = () => {
    return (
        <TodosVisualStyle>
            <h2>TOUR</h2>
        </TodosVisualStyle>
    );
};

export default TodosVisual;
